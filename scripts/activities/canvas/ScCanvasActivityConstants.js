export const CANVAS_TARGET_SOURCES = Object.freeze({
  TARGETS: "targets",
  CONTROLLED: "controlled",
  SELF: "self"
});

export const TELEPORT_DESTINATION_SOURCES = Object.freeze({
  ORIGIN: "origin",
  TARGET: "target"
});

export const MOVEMENT_TYPES = Object.freeze({
  PUSH: "push",
  PULL: "pull"
});

export const WALL_TARGET_SOURCES = Object.freeze({
  TARGETS: "targets",
  CONTROLLED: "controlled"
});
